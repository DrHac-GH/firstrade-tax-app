#!/bin/bash

# このスクリプトがあるディレクトリに移動
cd "$(dirname "$0")"

# 必要なパッケージがインストールされていない場合はインストール
if [ ! -d "node_modules" ]; then
    echo "初回起動準備中... (これには数分かかる場合があります)"
    npm install
fi

echo "アプリケーションを起動しています..."
echo "終了するには、このウィンドウを閉じてください。"

# アプリを起動 (ブラウザも自動で開きます)
npm run dev
